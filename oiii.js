document.addEventListener('DOMContentLoaded', function () {
    const contactForm = document.getElementById('contact-form');

    contactForm.addEventListener('submit', function (e) {
        e.preventDefault();

        // Realize as validações do formulário aqui
        // Exemplo: verificar se o e-mail é válido

        // Em seguida, você pode enviar os dados do formulário para um servidor ou realizar outras ações
    });
});
